# Default values for lp-reports.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: mpetason/launchpad_reports
  tag: latest
  pullPolicy: IfNotPresent
